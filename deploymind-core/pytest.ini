[pytest]
testpaths = tests
markers =
    unit: Unit tests (no external dependencies)
    aws: AWS integration tests (requires AWS credentials)
    github: GitHub integration tests (requires GitHub token)
    integration: End-to-end integration tests
    trivy: Trivy scanner integration tests (requires Trivy installed)
addopts = -v --tb=short
